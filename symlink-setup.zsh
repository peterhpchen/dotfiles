#!/bin.zsh

# Vim
sudo ln -sf $PWD/.vim ~/

# Alacritty
sudo ln -sf $PWD/alacritty ~/.config

# Zsh
sudo ln -sf $PWD/.zshrc ~/

# Zim
sudo ln -sf $PWD/.zimrc ~/

# Tmux
sudo ln -sf $PWD/.tmux ~/
sudo ln -sf $PWD/.tmux.conf ~/
